definitions:
  Booking:
    type: object
    properties:
      field_id:
        type: number
      play_date_start:
        type: string
        format: date-time
      play_date_end:
        type: string
        format: date-time
    required:
      - field_id
      - play_date_start
      - play_date_end
